import { useMemo } from 'react';

function Results({ gradeInfo, questions, answers, onRestart }) {
  const { score, profile, breakdown } = useMemo(() => {
    let correctCount = 0;
    const questionBreakdown = questions.map((q, index) => {
      const userAnswer = answers[index];
      const isCorrect = userAnswer?.correct || false;
      if (isCorrect) correctCount++;
      return {
        question: q.question,
        userAnswer: userAnswer?.text || 'Not answered',
        correct: isCorrect,
        correctAnswer: q.options.find(o => o.correct)?.text
      };
    });

    const matchingProfile = gradeInfo.profiles.find(
      p => correctCount >= p.minScore && correctCount <= p.maxScore
    ) || gradeInfo.profiles[0];

    return {
      score: correctCount,
      profile: matchingProfile,
      breakdown: questionBreakdown
    };
  }, [gradeInfo, questions, answers]);

  const percentage = (score / questions.length) * 100;

  const generateReport = () => {
    const date = new Date().toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });

    let report = `
╔═══════════════════════════════════════════════════════════════╗
║                    FuturAIse AI Readiness Report              ║
╚═══════════════════════════════════════════════════════════════╝

Date: ${date}
Grade Level: ${gradeInfo.label} (${gradeInfo.title})

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 YOUR LEARNING PROFILE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Profile: ${profile.name}
Score: ${score}/${questions.length} (${percentage}%)

${profile.description}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 QUESTION BREAKDOWN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

`;

    breakdown.forEach((item, index) => {
      report += `
Question ${index + 1}: ${item.correct ? '✓ Correct' : '✗ Incorrect'}
${item.question}

Your Answer: ${item.userAnswer}
${!item.correct ? `Correct Answer: ${item.correctAnswer}` : ''}
${'─'.repeat(60)}
`;
    });

    report += `

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 WHAT'S NEXT?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Focus Area: ${gradeInfo.focus}

Based on your profile as a "${profile.name}", here are some suggestions:

${score <= 2 ? `• Start with the basics of AI awareness
• Explore interactive AI tools and games
• Watch kid-friendly AI explanation videos` :
  score <= 4 ? `• Dive deeper into specific AI domains
• Try hands-on coding projects
• Explore AI ethics and real-world applications` :
  `• Challenge yourself with advanced AI concepts
• Start learning Python programming
• Consider joining AI clubs or competitions`}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Thank you for taking the FuturAIse AI Readiness Survey!
Visit us to learn more about our AI programs.

Generated by FuturAIse © ${new Date().getFullYear()}
`;

    return report;
  };

  const handleDownload = () => {
    const report = generateReport();
    const blob = new Blob([report], { type: 'text/plain' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `FuturAIse_AI_Report_${gradeInfo.label.replace(/\s+/g, '_')}.txt`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  };

  const getScoreColor = () => {
    if (percentage >= 80) return 'from-emerald-400 to-cyan-400';
    if (percentage >= 50) return 'from-amber-400 to-orange-400';
    return 'from-violet-400 to-pink-400';
  };

  const getProfileEmoji = () => {
    if (percentage >= 80) return '🌟';
    if (percentage >= 50) return '⚡';
    return '🚀';
  };

  return (
    <div className="min-h-screen flex flex-col items-center justify-center px-4 py-8 relative z-10">
      <div className="w-full max-w-2xl">
        {/* Celebration Header */}
        <div className="text-center mb-8 animate-float">
          <div className="text-6xl mb-4">{getProfileEmoji()}</div>
          <h1 className="text-3xl md:text-4xl font-bold text-white mb-2">
            Mission Complete!
          </h1>
          <p className="text-violet-200/70">
            You've finished the {gradeInfo.title} quiz
          </p>
        </div>

        {/* Profile Card */}
        <div className="bg-gradient-to-br from-slate-800/90 to-slate-900/90 backdrop-blur-sm rounded-3xl p-6 md:p-8 border border-violet-500/30 shadow-xl shadow-violet-500/10 mb-6">
          {/* Score Ring */}
          <div className="flex justify-center mb-8">
            <div className="relative w-40 h-40">
              {/* Background circle */}
              <svg className="w-full h-full transform -rotate-90">
                <circle
                  cx="80"
                  cy="80"
                  r="70"
                  stroke="currentColor"
                  strokeWidth="12"
                  fill="none"
                  className="text-slate-700/50"
                />
                <circle
                  cx="80"
                  cy="80"
                  r="70"
                  stroke="url(#scoreGradient)"
                  strokeWidth="12"
                  fill="none"
                  strokeLinecap="round"
                  strokeDasharray={`${percentage * 4.4} 440`}
                  className="transition-all duration-1000 ease-out"
                />
                <defs>
                  <linearGradient id="scoreGradient" x1="0%" y1="0%" x2="100%" y2="0%">
                    <stop offset="0%" stopColor="#8b5cf6" />
                    <stop offset="100%" stopColor="#06b6d4" />
                  </linearGradient>
                </defs>
              </svg>
              {/* Score text */}
              <div className="absolute inset-0 flex flex-col items-center justify-center">
                <span className={`text-4xl font-bold bg-gradient-to-r ${getScoreColor()} bg-clip-text text-transparent`}>
                  {score}/{questions.length}
                </span>
                <span className="text-violet-300/60 text-sm">Score</span>
              </div>
            </div>
          </div>

          {/* Profile Badge */}
          <div className="text-center mb-6">
            <div className="inline-block px-4 py-2 rounded-full bg-gradient-to-r from-violet-500/20 to-cyan-500/20 border border-violet-400/30 mb-4">
              <span className="text-lg font-semibold bg-gradient-to-r from-violet-300 to-cyan-300 bg-clip-text text-transparent">
                {profile.name}
              </span>
            </div>
            <p className="text-violet-100/80 max-w-md mx-auto">
              {profile.description}
            </p>
          </div>

          {/* Quick Stats */}
          <div className="grid grid-cols-3 gap-4 mb-6">
            <div className="bg-slate-700/30 rounded-xl p-4 text-center">
              <div className="text-2xl font-bold text-emerald-400">{score}</div>
              <div className="text-xs text-violet-300/60">Correct</div>
            </div>
            <div className="bg-slate-700/30 rounded-xl p-4 text-center">
              <div className="text-2xl font-bold text-amber-400">{questions.length - score}</div>
              <div className="text-xs text-violet-300/60">To Review</div>
            </div>
            <div className="bg-slate-700/30 rounded-xl p-4 text-center">
              <div className="text-2xl font-bold text-cyan-400">{percentage}%</div>
              <div className="text-xs text-violet-300/60">Accuracy</div>
            </div>
          </div>

          {/* Focus Area */}
          <div className="bg-gradient-to-r from-violet-500/10 to-cyan-500/10 rounded-xl p-4 border border-violet-500/20">
            <div className="flex items-center gap-3 mb-2">
              <span className="text-xl">{gradeInfo.emoji}</span>
              <span className="text-violet-300 font-medium">Your Focus Area</span>
            </div>
            <p className="text-violet-100/70 text-sm">{gradeInfo.focus}</p>
          </div>
        </div>

        {/* Question Breakdown */}
        <div className="bg-gradient-to-br from-slate-800/90 to-slate-900/90 backdrop-blur-sm rounded-3xl p-6 border border-violet-500/30 shadow-xl shadow-violet-500/10 mb-6">
          <h3 className="text-lg font-semibold text-white mb-4 flex items-center gap-2">
            <span>📋</span> Question Review
          </h3>
          <div className="space-y-3">
            {breakdown.map((item, index) => (
              <div
                key={index}
                className={`p-3 rounded-xl border ${
                  item.correct
                    ? 'bg-emerald-500/10 border-emerald-500/30'
                    : 'bg-red-500/10 border-red-500/30'
                }`}
              >
                <div className="flex items-start gap-3">
                  <div className={`w-6 h-6 rounded-full flex items-center justify-center shrink-0 ${
                    item.correct ? 'bg-emerald-500' : 'bg-red-500'
                  }`}>
                    {item.correct ? (
                      <svg className="w-4 h-4 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
                      </svg>
                    ) : (
                      <svg className="w-4 h-4 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
                      </svg>
                    )}
                  </div>
                  <div className="flex-1 min-w-0">
                    <p className="text-white/90 text-sm font-medium mb-1">Q{index + 1}: {item.question}</p>
                    <p className={`text-xs ${item.correct ? 'text-emerald-300/70' : 'text-red-300/70'}`}>
                      Your answer: {item.userAnswer}
                    </p>
                    {!item.correct && (
                      <p className="text-cyan-300/70 text-xs mt-1">
                        Correct: {item.correctAnswer}
                      </p>
                    )}
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Action Buttons */}
        <div className="flex flex-col sm:flex-row gap-4">
          <button
            onClick={handleDownload}
            className="flex-1 flex items-center justify-center gap-2 px-6 py-4 bg-gradient-to-r from-violet-500 to-cyan-500 text-white font-semibold rounded-xl hover:from-violet-600 hover:to-cyan-600 transition-all shadow-lg shadow-violet-500/30 hover:shadow-violet-500/50"
          >
            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
            </svg>
            Download Report
          </button>
          <button
            onClick={onRestart}
            className="flex-1 flex items-center justify-center gap-2 px-6 py-4 bg-slate-700/50 text-violet-200 font-semibold rounded-xl border border-violet-500/30 hover:bg-slate-700/70 hover:border-violet-400/50 transition-all"
          >
            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
            </svg>
            Try Another Level
          </button>
        </div>

        {/* Footer */}
        <div className="mt-8 text-center">
          <p className="text-violet-300/50 text-sm">
            Thank you for exploring AI with FuturAIse! 🚀
          </p>
        </div>
      </div>
    </div>
  );
}

export default Results;
